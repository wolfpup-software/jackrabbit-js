{
  "name": "jackrabbit",
  "workspaces": [
    "core",
    "cli",
    "nodejs_cli",
    "tests"
  ],
  "scripts": {
    "install": "npm run build",
    "build": "npm run --workspaces build",
    "fmt": "npx prettier --write ./",
    "test": "node ./nodejs_cli/bin/mod.js --file ./tests/dist/mod.js"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "prettier": "^3.3.1",
    "typescript": "^5.4.5"
  }
}
