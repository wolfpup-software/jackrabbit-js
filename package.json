{
    "workspaces": [
        "core"
    ],
    "scripts": {
        "build": "npm run --workspaces build",
        "fmt": "npx prettier --write ./"
    },
    "devDependencies": {
        "prettier": "^3.3.1",
        "typescript": "^5.4.5"
    }
}
